IF 1 == 2 => 
    var::x = 1
    var::y = MString(x)

void A(a) => 
    var::y = a + 1
    var::_i = MInt(y)
    Array[numbers] = [1,2,3,4,5]
    numbers.forEach(element, index) =>
        numbers[index] += 1
